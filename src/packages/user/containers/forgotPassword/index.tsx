import { useEffect } from 'react';

interface ForgotPasswordProps {}

export const ForgotPassword: React.FunctionComponent<ForgotPasswordProps> = ({}) => {
    useEffect(() => {}, []);

    return <div className="w-full">this is forgot password page</div>;
};
