import { useEffect } from 'react';

interface UpdatePasswordProps {}

export const UpdatePassword: React.FunctionComponent<UpdatePasswordProps> = ({}) => {
    useEffect(() => {}, []);

    return <div className="w-full">this is update password page</div>;
};
